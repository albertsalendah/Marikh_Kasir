<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#F7AA35</item>
        <item name="colorPrimaryDark">#F7AA35</item>
        <item name="colorAccent">#F7AA35</item>

        <!--        untuk dropdown menu-->
        <item name="android:popupMenuStyle">@style/PopupMenu</item>
        <!--<item name="android:dropDownListViewStyle">@style/DropDownListView</item>-->

    </style>

    <!--        untuk dropdown menu-->
    <style name="PopupMenu" parent="@android:style/Widget.Holo.ListPopupWindow">
        <item name="android:popupBackground">@drawable/border</item>
        <item name="android:itemPadding">1dp</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- <style name="DropDownListView" parent="@android:style/Widget.Holo.ListView.DropDown">
        <item name="android:listSelector">@drawable/border</item>
    </style>-->


</resources>
